apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: strip-api-prefix
  namespace: devops-test
spec:
  stripPrefix:
    prefixes:
      - /api/auth
      - /api/catalog
      - /api/order
